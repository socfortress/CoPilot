/**
 * The one definition of a section label — the small uppercase caption that titles
 * a card, a fact cell or a list. It lives in `common` because common components
 * render it too, and a second copy is how a type scale starts to drift.
 */
export const SECTION_LABEL = "text-secondary text-xs font-medium tracking-wider uppercase"
