!frontend/src/components/logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.DS_Store
dist
coverage
*.local

cypress/videos/
cypress/screenshots/
report/

src/unplugin.components.d.ts
cypress/screenshots/
cypress/downloads/
tsconfig.app.tsbuildinfo
tsconfig.node.tsbuildinfo
tsconfig.vitest.tsbuildinfo

# Playwright (e2e/)
test-results/
playwright-report/
blob-report/
playwright/.cache/
tsconfig.e2e.tsbuildinfo
# resolved by e2e/global-setup.ts at run time, differs per deployment
e2e/.tenants.json
# holds the admin credential the suite seeds with
.env.e2e
# ...but the template must ship: the repo root ignores `.env.*`, which would swallow it
!.env.e2e.example
